{
  "name": "calendar-client-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project tsconfig.json",
    "start:dev": "ts-node -r dotenv/config src/index.ts",
    "buildAndStart": "cd ../frontend && npm run build && cd ../backend && npm run start:dev",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "@types/node-fetch": "^2.5.7",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "ts-node": "^9.0.0"
  }
}
